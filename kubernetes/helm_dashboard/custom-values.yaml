dashboard:
  persistence:
    storageClass: nfs-client
ingress:
  enabled: true 
  className: "nginx"
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
  hosts:
    - paths:
        - path: /helm
          pathType: Prefix
